# Extension signing keys — never commit these
*.pem
*.crx
key.json

# Secrets / local env
.env
.env.*
*.local

# OS cruft
.DS_Store
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp

# Node (in case tooling is added later)
node_modules/
npm-debug.log*

# Claude Code local/session config — not meant to be shared via the repo
.claude/
.claude.json
.mcp.json
CLAUDE.md
